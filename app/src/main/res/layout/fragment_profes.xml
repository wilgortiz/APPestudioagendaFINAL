<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Contenedor principal con LinearLayout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Apartado para el periodo de las materias -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Periodo: "
                android:textColor="#FFFFFF" />
<!--
            <Spinner
                android:id="@+id/spPeriodoProfes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:entries="@array/periodosProfes" />
-->
        </LinearLayout>

        <!-- RecyclerView para mostrar los items -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvProfes"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>

    <!-- Botón flotante para agregar materia -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/fabAgregarProfes"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="30dp"
        android:backgroundTint="#FF9800"
        android:insetLeft="0dp"
        android:insetTop="0dp"
        android:insetRight="0dp"
        android:insetBottom="0dp"
        android:text="+"
        android:textSize="36sp"
        app:cornerRadius="16dp"
        app:elevation="6dp"
        app:iconGravity="textTop"
        app:iconTint="@android:color/white" />

</FrameLayout>